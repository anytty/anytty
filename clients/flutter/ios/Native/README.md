# AnyTTY native iOS libraries

Run `scripts/build-flutter-ios-native.sh` from the repository root after a full
Xcode installation. It generates `AnyTTYClient.xcframework` and
`AnyTTYTerminalInput.xcframework` in this directory. Generated XCFrameworks are
ignored by Git.
