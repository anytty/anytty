// Package conformance owns the frozen wire fixtures of the access product
// line. The fixtures snapshot the wire behavior already shipped to the
// Flutter App so that refactors cannot silently change it; the package also
// hosts the layout/dependency guard tests for the repository.
//
// The fixture implementation is intentionally empty for now: the directory
// and its contract are fixed first, the snapshots follow.
package conformance
