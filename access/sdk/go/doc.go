// Package sdk is the Go entry of the access SDK thin shell. It re-exports the
// stable access engine surface for external Go consumers; it must not grow
// behavior beyond that surface.
package sdk
