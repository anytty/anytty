# AnyTTY PTY adapter

This package is derived from `github.com/aymanbagabas/go-pty` v0.2.3.
The upstream code is MIT licensed; see `LICENSE`.

AnyTTY owns this copy so its Windows implementation can prefer the bundled
Windows Terminal ConPTY runtime. Other platform behavior remains aligned with
the upstream version.
