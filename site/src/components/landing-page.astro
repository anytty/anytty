---
import {
  ArrowDownToLine,
  ArrowRight,
  CheckCircle2,
  Clock3,
  Cloud,
  Eye,
  FileStack,
  Files,
  GitFork,
  History,
  Laptop,
  LockKeyhole,
  MonitorSmartphone,
  PanelsTopLeft,
  Radio,
  ShieldCheck,
  Smartphone,
  TerminalSquare,
} from 'lucide-react'

import { InstallTabs } from '@/components/install-tabs'
import { Badge } from '@/components/ui/badge'
import { buttonVariants } from '@/components/ui/button'
import { Card, CardContent, CardHeader } from '@/components/ui/card'
import { localizedPath, sitePath, type Locale } from '@/lib/site'
import { cn } from '@/lib/utils'

interface Props { locale: Locale }
const { locale } = Astro.props

const copy = {
  en: {
    eyebrow: 'Open source · macOS · Linux · Windows · Android · iOS',
    promise: 'Keep terminals running. Come back whenever you need to.',
    lead: 'AnyTTY lets you view and control running terminals from your desktop, phone, or tablet. Closing a window, switching devices, or losing a connection won\'t stop the work.',
    quickStart: 'Quick start', github: 'GitHub', release: 'Download beta',
    installLabel: 'macOS / Linux', windowsInstallLabel: 'Windows', installPlatformLabel: 'Choose an installer',
    installCommand: 'curl -fsSL https://raw.githubusercontent.com/anytty/anytty/main/install.sh | sh',
    windowsCommand: 'irm https://raw.githubusercontent.com/anytty/anytty/main/install.ps1 -OutFile install.ps1; ./install.ps1',
    copyCommand: 'Copy install command', copied: 'Copied',
    previewLabel: 'A real AnyTTY workspace', previewMeta: 'local + remote · 5 terminals · one workspace',
    previewAlt: 'AnyTTY TUI showing local and remote terminals in split and floating panels',
    observerEyebrow: 'Close the window, keep the work', observerTitle: 'The window is just a view. The terminal keeps running behind it.',
    observerLead: 'AnyTTY keeps terminals separate from the windows that view them. Switch which terminal a panel shows without rebuilding your layout or restarting the task.',
    runtime: 'Terminal runtime', runtimeState: 'Keeps running on your machine', snapshots: 'Screen snapshots',
    views: ['TUI panel', 'CLI', 'WebView', 'Mobile'],
    observerPoints: [
      ['Switch terminals without touching your layout', 'The Terminal Picker changes what the current panel observes while your splits, floats, and workspace stay exactly where they are.'],
      ['A slow phone won\'t slow the task', 'Each device reads snapshots at its own pace, so a slow network or phone never makes the process inside the terminal wait.'],
    ],
    workbenchEyebrow: 'One workbench', workbenchTitle: 'Local and remote terminals, managed together.',
    workbenchLead: 'Windows, macOS, and Linux are all first-class. Local, SSH, and Direct terminals show up in the same picker and behave like one workspace.',
    windowsAlt: 'AnyTTY TUI running on Windows with multiple terminals',
    workbenchPoints: [
      ['Native Windows support', 'CLI, TUI, and daemon builds for x64 and ARM64, with no WSL required for core workflows.'],
      ['Remote machines feel local', 'Operate a workstation, build machine, or remote AI agent the same way you operate a local terminal.'],
      ['Spot stalled work fast', 'See which terminal stopped producing output and find stuck tasks or agents without opening every session.'],
    ],
    historyEyebrow: 'History that outlives the screen', historyTitle: 'Long-running work shouldn\'t mean ever-growing memory.',
    historyLead: 'Output is written to files while the live view stays bounded. There is no fixed history line limit; practical capacity is limited by disk space, so history can keep growing as long as disk capacity does.',
    historyStats: [['Live memory', 'Bounded'], ['History', 'Disk-limited'], ['Reconnect', 'Full context']],
    historyNote: 'Useful after a disconnect, an accidental exit, or an unattended agent run that produced hours of output.',
    mobileEyebrow: 'Android + iOS', mobileTitle: 'See what\'s alive on your phone. Step in when it matters.',
    mobileLead: 'The mobile app is built for terminal work, not a desktop page squeezed onto a phone. Check recent activity, attach with a touch-optimized key row, and manage files over the same connection.',
    mobilePoints: [
      ['Terminal activity at a glance', 'Scan running sessions and time since last output.'],
      ['Touch-ready controls', 'Use Esc, Ctrl, arrows, paging, and common terminal actions.'],
      ['Files and previews', 'Browse, upload, download, and preview remote files.'],
    ],
    mobileAlts: ['AnyTTY mobile terminal list with recent activity', 'AnyTTY mobile terminal with an extra-key row', 'AnyTTY mobile remote file manager'],
    accessEyebrow: 'Reach your machines without loosening the boundary', accessTitle: 'Connect to what you own, keep the permissions yours.',
    accessLead: 'Use SSH when it already fits. When a machine sits behind NAT, the optional AnyTTY Cloud service can establish a peer-to-peer path or relay encrypted traffic while the local service keeps terminal and file permissions.',
    accessPoints: [
      ['Reach private-network machines', 'Connect through P2P hole punching when possible, with encrypted relay as the fallback.'],
      ['Encrypted by default', 'Remote terminal and file traffic is authenticated and encrypted instead of trusting the network path.'],
      ['Files travel with the session', 'Browse, upload, download, and manage files through the same protected connection.'],
    ],
    tabletEyebrow: 'Tablet workflows', tabletTitle: 'A full working surface when a phone isn\'t enough.',
    tabletLead: 'Run split-screen agent workflows or use terminal-native tools directly from the tablet client.',
    tabletCaptions: ['Codex and OpenCode in a vertical split', 'The TTT editor running inside a terminal'],
    tabletAlts: ['Tablet split screen running Codex and OpenCode', 'TTT editor running inside an AnyTTY terminal on a tablet'],
    installEyebrow: 'Beta 0.0.1', installTitle: 'Run it on the machine you already use.',
    installLead: 'The release includes binaries for macOS, Linux, and Windows, plus an unsigned Android beta APK. iOS is supported; public distribution is waiting for the App Store. Google Play and App Store submissions are in progress.',
    docs: 'Read the documentation', releases: 'Open GitHub Releases',
    platforms: ['macOS', 'Linux', 'Windows', 'Android', 'iOS'],
    openSource: 'Apache-2.0 · source, clients, and release artifacts on GitHub',
  },
  'zh-CN': {
    eyebrow: '开源 · macOS · Linux · Windows · Android · iOS',
    promise: '终端一直跑着，你随时回来接管。',
    lead: 'AnyTTY 让你在电脑、手机或平板上查看和操作正在运行的终端。关掉窗口、切换设备或网络断开，都不会让任务停下来。',
    quickStart: '快速开始', github: 'GitHub', release: '下载 Beta',
    installLabel: 'macOS / Linux', windowsInstallLabel: 'Windows', installPlatformLabel: '选择安装平台',
    installCommand: 'curl -fsSL https://raw.githubusercontent.com/anytty/anytty/main/install.sh | sh',
    windowsCommand: 'irm https://raw.githubusercontent.com/anytty/anytty/main/install.ps1 -OutFile install.ps1; ./install.ps1',
    copyCommand: '复制安装命令', copied: '已复制',
    previewLabel: '真实的 AnyTTY 工作台', previewMeta: '本地 + 远程 · 5 个终端 · 一个工作区',
    previewAlt: 'AnyTTY TUI 通过分屏和浮动 Panel 同时展示本地与远程终端',
    observerEyebrow: '关掉窗口，任务不中断', observerTitle: '窗口只是入口，终端还在背后跑。',
    observerLead: 'AnyTTY 把终端和查看它的窗口分开。你可以随时切换当前窗口在看哪个终端，不用重建布局，也不用重启正在跑的任务。',
    runtime: '终端本体', runtimeState: '一直跑在你的机器上', snapshots: '画面快照',
    views: ['TUI 面板', 'CLI', 'WebView', '手机 App'],
    observerPoints: [
      ['想切哪个终端就切哪个', '切换当前窗口观察的终端时，分屏、浮动窗和工作区布局都保持不变。'],
      ['手机慢也不拖累任务', '每个设备按自己的速度读取画面。手机或网络慢一点，不会让终端里的程序停下来等。'],
    ],
    workbenchEyebrow: '一个工作台', workbenchTitle: '本地和远程终端，放在一起管理。',
    workbenchLead: 'Windows、macOS、Linux 都能用。本地终端、SSH 远程终端和直连终端都进同一个列表，操作方式一致。',
    windowsAlt: 'AnyTTY TUI 在 Windows 上管理多个终端',
    workbenchPoints: [
      ['Windows 原生支持', 'CLI、TUI 和后台服务都有 x64 与 ARM64 版本，核心功能不需要 WSL。'],
      ['远程机器像本地一样操作', '工作站、构建机或远程 Agent，都能像本地终端一样直接进入工作流。'],
      ['一眼看到谁卡住了', '按最近输出排序，快速找到长时间没有动静的终端或任务。'],
    ],
    historyEyebrow: '历史记录不丢', historyTitle: '跑很久，也不怕内存越占越多。',
    historyLead: '终端输出会写入文件，实时画面只保留有限数据。历史记录没有固定行数上限，主要受磁盘空间限制，跑多久都能继续记。',
    historyStats: [['实时内存', '有界'], ['历史容量', '看磁盘'], ['重新连接', '完整上下文']],
    historyNote: '断线、误关窗口，或者 Agent 跑了好几个小时，重新连上后还能看到完整现场。',
    mobileEyebrow: 'Android + iOS', mobileTitle: '手机上先看状态，再决定要不要接管。',
    mobileLead: '手机 App 不是把桌面页面硬塞进小屏幕。你可以快速查看哪些终端还在跑，用触控优化的按键操作，也能直接管理远程文件。',
    mobilePoints: [
      ['终端状态一眼看清', '查看运行中的终端和距离上次输出的时间。'],
      ['触控操作顺手', 'Esc、Ctrl、方向键、翻页和常用终端操作都做了触控适配。'],
      ['文件也能管', '浏览、上传、下载和预览远程文件。'],
    ],
    mobileAlts: ['AnyTTY 手机 App 展示终端列表与近期活动', 'AnyTTY 手机 App 使用扩展按键操作终端', 'AnyTTY 手机 App 的远程文件管理器'],
    accessEyebrow: '连接更简单，边界不放松', accessTitle: '能连上自己的机器，权限还是你的。',
    accessLead: '能用 SSH 就直接用 SSH。机器在内网时，AnyTTY Cloud 会尝试 P2P 直连，连不上就自动走加密中继；终端和文件权限仍然由本机后台服务控制。',
    accessPoints: [
      ['内网也能连', '优先 P2P 直连，连不上时自动使用加密中继。'],
      ['默认加密', '远程终端和文件流量都经过身份验证和加密。'],
      ['文件和终端走同一条连接', '浏览、上传、下载和管理文件，不用再单独开一条通道。'],
    ],
    tabletEyebrow: '平板工作流', tabletTitle: '手机不够用，就上平板。',
    tabletLead: '可以上下分屏同时跑两个 Agent，也可以在平板客户端里直接使用终端原生工具。',
    tabletCaptions: ['上下分屏同时运行 Codex 与 OpenCode', '在终端里运行 TTT 编辑器'],
    tabletAlts: ['平板上下分屏运行 Codex 与 OpenCode', '在平板 AnyTTY 终端内运行 TTT 编辑器'],
    installEyebrow: 'Beta 0.0.1', installTitle: '直接装在你现在用的机器上。',
    installLead: '目前提供 macOS、Linux、Windows 安装包，以及未签名的 Android Beta APK。iOS 已支持，但公开安装要等 App Store；Google Play 和 App Store 都在推进中。',
    docs: '阅读文档', releases: '查看 GitHub Releases',
    platforms: ['macOS', 'Linux', 'Windows', 'Android', 'iOS'],
    openSource: 'Apache-2.0 · 源码、客户端和构建产物都在 GitHub',
  },
} as const

const t = copy[locale]
const docsPath = sitePath(localizedPath(locale, '/docs/quick-start'))
const releaseUrl = 'https://github.com/anytty/anytty/releases/tag/v0.0.1-beta.0'
const repoUrl = 'https://github.com/anytty/anytty'
const viewIcons = [PanelsTopLeft, TerminalSquare, Laptop, Smartphone]
const observerIcons = [Eye, ShieldCheck]
const workbenchIcons = [MonitorSmartphone, Laptop, Clock3]
const mobileIcons = [Clock3, TerminalSquare, FileStack]
const accessIcons = [Cloud, LockKeyhole, Files]
---

<main id="main">
  <section class="dark overflow-hidden bg-zinc-950 text-zinc-50" aria-labelledby="product-title">
    <div class="mx-auto max-w-7xl px-4 pb-16 pt-16 sm:px-6 sm:pt-20 lg:px-8 lg:pt-24">
      <div class="mx-auto flex max-w-4xl flex-col items-center text-center">
        <img class="size-16 rounded-lg shadow-lg ring-1 ring-white/15" src={sitePath('/assets/app-icon.png')} alt="" width="64" height="64" />
        <Badge variant="secondary" className="mt-6 border-0 bg-white/10 px-3 py-1 font-mono text-[11px] font-normal text-zinc-200 shadow-none">
          {t.eyebrow}
        </Badge>
        <h1 id="product-title" class="mt-5 text-5xl font-medium leading-none sm:text-6xl">AnyTTY</h1>
        <p class="mt-5 text-2xl font-medium leading-snug text-zinc-100 sm:text-3xl">{t.promise}</p>
        <p class="mt-5 max-w-2xl text-base font-normal leading-7 text-zinc-400">{t.lead}</p>
        <div class="mt-8 flex flex-wrap items-center justify-center gap-3">
          <a class={cn(buttonVariants({ variant: 'default', size: 'lg' }), 'h-11 px-5 font-normal')} href={docsPath}>
            {t.quickStart}<ArrowRight aria-hidden="true" />
          </a>
          <a class={cn(buttonVariants({ variant: 'outline', size: 'lg' }), 'h-11 border-white/15 bg-white/5 px-5 font-normal text-zinc-100 hover:bg-white/10 hover:text-white')} href={repoUrl}>
            <GitFork aria-hidden="true" />{t.github}
          </a>
          <a class={cn(buttonVariants({ variant: 'ghost', size: 'lg' }), 'h-11 px-4 font-normal text-zinc-300 hover:bg-white/10 hover:text-white')} href={releaseUrl}>
            <ArrowDownToLine aria-hidden="true" />{t.release}
          </a>
        </div>
        <div class="mt-8 w-full max-w-3xl text-left">
          <InstallTabs
            client:load
            ariaLabel={t.installPlatformLabel}
            unixLabel={t.installLabel}
            windowsLabel={t.windowsInstallLabel}
            unixCommand={t.installCommand}
            windowsCommand={t.windowsCommand}
            copyLabel={t.copyCommand}
            copiedLabel={t.copied}
          />
        </div>
      </div>

      <figure class="mt-14 overflow-hidden rounded-lg bg-black shadow-2xl ring-1 ring-white/10">
        <figcaption class="flex min-h-12 flex-col justify-center gap-1 px-4 font-mono text-[11px] font-normal text-zinc-400 sm:flex-row sm:items-center sm:justify-between sm:gap-6">
          <span class="flex items-center gap-2 text-zinc-200"><Radio className="size-3.5 text-emerald-400" aria-hidden="true" />{t.previewLabel}</span>
          <span>{t.previewMeta}</span>
        </figcaption>
        <img class="block aspect-[16/10] w-full object-cover object-top" src={sitePath('/assets/product/mac-tui.webp')} alt={t.previewAlt} width="1800" height="1125" />
      </figure>
    </div>
  </section>

  <section id="features" class="scroll-mt-20 bg-card py-20 sm:py-24 lg:py-28" aria-labelledby="observer-title">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="grid gap-8 lg:grid-cols-[0.7fr_1.3fr] lg:gap-16">
        <div class="min-w-0">
          <Badge variant="secondary" className="font-mono text-[11px] font-normal">{t.observerEyebrow}</Badge>
        </div>
        <div class="min-w-0">
          <h2 id="observer-title" class="max-w-4xl text-3xl font-medium leading-tight sm:text-4xl">{t.observerTitle}</h2>
          <p class="mt-5 max-w-3xl text-base font-normal leading-7 text-muted-foreground">{t.observerLead}</p>
        </div>
      </div>

      <div class="mt-14 flex flex-col items-stretch gap-5 lg:flex-row lg:items-center">
        <Card className="min-h-32 flex-1 border-0 bg-secondary shadow-none">
          <CardContent className="flex min-h-32 items-center gap-4 p-6">
            <span class="grid size-11 shrink-0 place-items-center rounded-lg bg-primary text-primary-foreground"><TerminalSquare aria-hidden="true" /></span>
            <span><strong class="block text-base font-medium">{t.runtime}</strong><span class="mt-1 block text-sm font-normal text-muted-foreground">{t.runtimeState}</span></span>
          </CardContent>
        </Card>
        <div class="flex shrink-0 items-center justify-center gap-2 font-mono text-[11px] font-normal text-muted-foreground">
          <span>{t.snapshots}</span><ArrowRight className="size-4 rotate-90 text-primary lg:rotate-0" aria-hidden="true" />
        </div>
        <div class="grid flex-[2] grid-cols-2 gap-3 sm:grid-cols-4">
          {t.views.map((view, index) => {
            const Icon = viewIcons[index]
            return (
              <Card className="border-0 bg-muted/70 shadow-none">
                <CardContent className="flex min-h-32 flex-col items-center justify-center gap-3 p-4 text-center">
                  <Icon className="size-5 text-foreground" aria-hidden="true" />
                  <span class="font-mono text-[11px] font-normal text-muted-foreground">{view}</span>
                </CardContent>
              </Card>
            )
          })}
        </div>
      </div>

      <div class="mt-6 grid gap-4 md:grid-cols-2">
        {t.observerPoints.map(([title, body], index) => {
          const Icon = observerIcons[index]
          return (
            <Card className="border-0 bg-background shadow-none">
              <CardHeader className="flex-row items-start gap-4 space-y-0 p-6">
                <span class="grid size-10 shrink-0 place-items-center rounded-lg bg-secondary text-primary"><Icon className="size-5" aria-hidden="true" /></span>
                <div><h3 class="text-base font-medium">{title}</h3><p class="mt-2 text-sm font-normal leading-6 text-muted-foreground">{body}</p></div>
              </CardHeader>
            </Card>
          )
        })}
      </div>
    </div>
  </section>

  <section class="dark bg-zinc-950 py-20 text-zinc-50 sm:py-24 lg:py-28" aria-labelledby="workbench-title">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="grid gap-8 lg:grid-cols-[0.7fr_1.3fr] lg:gap-16">
        <div><Badge variant="secondary" className="border-0 bg-white/10 font-mono text-[11px] font-normal text-zinc-200">{t.workbenchEyebrow}</Badge></div>
        <div><h2 id="workbench-title" class="max-w-4xl text-3xl font-medium leading-tight sm:text-4xl">{t.workbenchTitle}</h2><p class="mt-5 max-w-3xl text-base font-normal leading-7 text-zinc-400">{t.workbenchLead}</p></div>
      </div>
      <img class="mt-14 block aspect-[1800/955] w-full rounded-lg object-cover shadow-2xl ring-1 ring-white/10" src={sitePath('/assets/product/win-tui.webp')} alt={t.windowsAlt} width="1800" height="955" loading="lazy" />
      <div class="mt-5 grid gap-4 md:grid-cols-3">
        {t.workbenchPoints.map(([title, body], index) => {
          const Icon = workbenchIcons[index]
          const iconColor = index === 0 ? 'text-sky-300' : index === 1 ? 'text-emerald-300' : 'text-amber-300'
          return (
            <Card className="border-white/10 bg-white/5 text-zinc-50 shadow-none">
              <CardHeader className="p-6"><Icon className={cn('size-5', iconColor)} aria-hidden="true" /><h3 class="mt-5 text-base font-medium">{title}</h3><p class="mt-2 text-sm font-normal leading-6 text-zinc-400">{body}</p></CardHeader>
            </Card>
          )
        })}
      </div>
    </div>
  </section>

  <section class="bg-sky-50/60 py-20 sm:py-24 lg:py-28" aria-labelledby="history-title">
    <div class="mx-auto grid max-w-7xl gap-12 px-4 sm:px-6 lg:grid-cols-[1.2fr_0.8fr] lg:items-end lg:gap-20 lg:px-8">
      <div>
        <Badge variant="outline" className="border-sky-200 bg-white/70 font-mono text-[11px] font-normal text-sky-800">{t.historyEyebrow}</Badge>
        <h2 id="history-title" class="mt-5 max-w-3xl text-3xl font-medium leading-tight sm:text-4xl">{t.historyTitle}</h2>
        <p class="mt-5 max-w-3xl text-base font-normal leading-7 text-muted-foreground">{t.historyLead}</p>
        <div class="mt-7 flex max-w-3xl items-start gap-3 rounded-lg bg-white/80 p-5 text-sm font-normal leading-6 text-slate-700 shadow-sm">
          <History className="mt-0.5 size-5 shrink-0 text-sky-700" aria-hidden="true" />{t.historyNote}
        </div>
      </div>
      <dl class="grid gap-3 sm:grid-cols-3 lg:grid-cols-1">
        {t.historyStats.map(([term, detail]) => (
          <div class="flex min-h-20 items-center justify-between gap-6 rounded-lg bg-white/80 px-5 py-4 shadow-sm">
            <dt class="font-mono text-[11px] font-normal text-muted-foreground">{term}</dt><dd class="text-lg font-medium">{detail}</dd>
          </div>
        ))}
      </dl>
    </div>
  </section>

  <section id="screens" class="scroll-mt-20 bg-background py-20 sm:py-24 lg:py-28" aria-labelledby="mobile-title">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="grid gap-10 lg:grid-cols-[1.05fr_0.95fr] lg:gap-20">
        <div><Badge variant="secondary" className="font-mono text-[11px] font-normal">{t.mobileEyebrow}</Badge><h2 id="mobile-title" class="mt-5 max-w-3xl text-3xl font-medium leading-tight sm:text-4xl">{t.mobileTitle}</h2><p class="mt-5 max-w-2xl text-base font-normal leading-7 text-muted-foreground">{t.mobileLead}</p></div>
        <div class="grid gap-5">
          {t.mobilePoints.map(([title, body], index) => {
            const Icon = mobileIcons[index]
            return <div class="flex items-start gap-4"><span class="grid size-10 shrink-0 place-items-center rounded-lg bg-secondary text-primary"><Icon className="size-5" aria-hidden="true" /></span><div><h3 class="text-sm font-medium">{title}</h3><p class="mt-1 text-sm font-normal leading-6 text-muted-foreground">{body}</p></div></div>
          })}
        </div>
      </div>
      <div class="mx-auto mt-14 grid max-w-5xl grid-cols-1 gap-5 sm:grid-cols-3">
        {['mobile-terminals.webp', 'mobile-terminal.webp', 'mobile-files.webp'].map((image, index) => (
          <figure class="overflow-hidden rounded-lg bg-card shadow-xl ring-1 ring-border/70">
            <img class="block aspect-[480/1068] w-full object-cover" src={sitePath(`/assets/product/${image}`)} alt={t.mobileAlts[index]} width="480" height="1068" loading="lazy" />
          </figure>
        ))}
      </div>
    </div>
  </section>

  <section class="bg-card py-20 sm:py-24 lg:py-28" aria-labelledby="access-title">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="mx-auto max-w-3xl text-center">
        <Badge variant="outline" className="font-mono text-[11px] font-normal text-primary">{t.accessEyebrow}</Badge>
        <h2 id="access-title" class="mt-5 text-3xl font-medium leading-tight sm:text-4xl">{t.accessTitle}</h2>
        <p class="mt-5 text-base font-normal leading-7 text-muted-foreground">{t.accessLead}</p>
      </div>
      <div class="mt-12 grid gap-4 md:grid-cols-3">
        {t.accessPoints.map(([title, body], index) => {
          const Icon = accessIcons[index]
          const tone = index === 0 ? 'bg-sky-100 text-sky-700' : index === 1 ? 'bg-emerald-100 text-emerald-700' : 'bg-amber-100 text-amber-800'
          return (
            <Card className="border-0 bg-muted/55 shadow-none">
              <CardHeader className="p-6"><span class={cn('grid size-11 place-items-center rounded-lg', tone)}><Icon aria-hidden="true" /></span><h3 class="mt-5 text-base font-medium">{title}</h3><p class="mt-2 text-sm font-normal leading-6 text-muted-foreground">{body}</p></CardHeader>
            </Card>
          )
        })}
      </div>
    </div>
  </section>

  <section class="bg-muted/50 py-20 sm:py-24 lg:py-28" aria-labelledby="tablet-title">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="grid gap-8 lg:grid-cols-[0.7fr_1.3fr] lg:gap-16">
        <div><Badge variant="outline" className="bg-background font-mono text-[11px] font-normal">{t.tabletEyebrow}</Badge></div>
        <div><h2 id="tablet-title" class="max-w-4xl text-3xl font-medium leading-tight sm:text-4xl">{t.tabletTitle}</h2><p class="mt-5 max-w-3xl text-base font-normal leading-7 text-muted-foreground">{t.tabletLead}</p></div>
      </div>
      <div class="mt-14 grid gap-6 md:grid-cols-2">
        {['pad-agents.webp', 'pad-ttt-editor.webp'].map((image, index) => (
          <figure>
            <img class="block w-full rounded-lg object-cover shadow-xl ring-1 ring-border/70" src={sitePath(`/assets/product/${image}`)} alt={t.tabletAlts[index]} width="900" height={index === 0 ? '1179' : '1146'} loading="lazy" />
            <figcaption class="mt-3 text-sm font-normal text-muted-foreground">{t.tabletCaptions[index]}</figcaption>
          </figure>
        ))}
      </div>
    </div>
  </section>

  <section id="install" class="dark scroll-mt-20 bg-zinc-950 py-20 text-zinc-50 sm:py-24 lg:py-28" aria-labelledby="install-title">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="grid gap-10 lg:grid-cols-[0.8fr_1.2fr] lg:gap-20">
        <div class="min-w-0">
          <Badge variant="secondary" className="border-0 bg-white/10 font-mono text-[11px] font-normal text-zinc-200">{t.installEyebrow}</Badge>
          <h2 id="install-title" class="mt-5 text-3xl font-medium leading-tight sm:text-4xl">{t.installTitle}</h2>
          <p class="mt-5 text-base font-normal leading-7 text-zinc-400">{t.installLead}</p>
          <div class="mt-7 flex flex-wrap gap-2" aria-label={t.platforms.join(', ')}>
            {t.platforms.map((platform) => <Badge variant="outline" className="border-white/15 bg-white/5 font-mono text-[11px] font-normal text-zinc-300">{platform}</Badge>)}
          </div>
        </div>
        <div class="min-w-0">
          <InstallTabs
            client:load
            ariaLabel={t.installPlatformLabel}
            unixLabel={t.installLabel}
            windowsLabel={t.windowsInstallLabel}
            unixCommand={t.installCommand}
            windowsCommand={t.windowsCommand}
            copyLabel={t.copyCommand}
            copiedLabel={t.copied}
          />
          <p class="mt-4 flex items-start gap-2 text-sm font-normal leading-6 text-zinc-400"><CheckCircle2 className="mt-0.5 size-4 shrink-0 text-emerald-400" aria-hidden="true" />{t.openSource}</p>
          <div class="mt-8 flex flex-wrap gap-3">
            <a class={cn(buttonVariants({ variant: 'default', size: 'lg' }), 'h-11 px-5 font-normal')} href={docsPath}>{t.docs}<ArrowRight aria-hidden="true" /></a>
            <a class={cn(buttonVariants({ variant: 'outline', size: 'lg' }), 'h-11 border-white/15 bg-white/5 px-5 font-normal text-zinc-100 hover:bg-white/10 hover:text-white')} href={releaseUrl}>{t.releases}<ArrowDownToLine aria-hidden="true" /></a>
          </div>
        </div>
      </div>
    </div>
  </section>
</main>
