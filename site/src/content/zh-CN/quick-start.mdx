---
title: 快速开始
description: 安装 AnyTTY，启动本地 daemon 和终端。
---

# 快速开始

安装当前 Beta，启动当前用户的 daemon，然后进入 TUI。

## 安装

macOS 或 Linux 可使用自动选择平台并校验 SHA-256 的安装脚本：

```sh
curl -fsSL https://raw.githubusercontent.com/anytty/anytty/main/install.sh | sh
```

Windows PowerShell：

```powershell
Invoke-WebRequest https://raw.githubusercontent.com/anytty/anytty/main/install.ps1 -OutFile install.ps1
.\install.ps1
```

## 本地运行

```sh
anytty daemon start
anytty
```

第二条命令针对默认 Local endpoint 打开 TUI。也可以直接从 CLI 创建 shell 并附加：

```sh
anytty terminal create --name workspace --attach -- zsh
```

## 从源码构建

源码构建需要 Go 1.26.5、Node.js 24 和 npm。

```sh
git clone https://github.com/anytty/anytty.git
cd anytty
npm ci
make build
make test
make test-clients
npm run public:check
```

> **需要连接其他机器？** 已有主机访问能力时优先使用 SSH；也可以配对支持 Direct 或 Cloud 的服务并保存为 endpoint。
