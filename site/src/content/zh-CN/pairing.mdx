---
title: 扫码配对
description: 通过一次性、客户端绑定的 claim 将客户端与 daemon 配对。
---

# 扫码配对

AnyTTY 移动 App 可通过扫描二维码或粘贴目标 daemon 生成的配对 claim 添加 endpoint。Direct 配对不需要账号。

import { sitePath } from "@/lib/site";

<img src={sitePath("/assets/mobile-terminal.png")} alt="与演示工作站配对后的 AnyTTY Android 终端" />

## 信任流程

1. **daemon 创建短期 claim。** offer 绑定 daemon identity、请求的 capability 和单次 enrollment 流程。
2. **客户端扫描或粘贴 claim，并证明身份。** 原生客户端把 endpoint 数据与凭据写入平台保护的存储。
3. **daemon 签发客户端绑定的访问权限。** claim 消费状态会被记录，同一 claim 不能重放。

> **未兑换的二维码应视为秘密。** 不要把 pairing URI 放入日志、截图、issue、聊天或版本控制。客户端丢失或不再可信时，应从 daemon 撤销 grant。

配对与撤销的安全承诺见仓库中的[安全边界](https://github.com/anytty/anytty/blob/main/docs/zh-CN/SECURITY_BOUNDARY.md)。
