---
title: QR pairing
description: Pair a client to a daemon with a one-time client-bound claim.
---

# QR pairing

AnyTTY mobile apps add an endpoint when you scan a QR code or paste a pairing claim generated by the target daemon. Direct pairing does not require an account.

import { sitePath } from "@/lib/site";

<img src={sitePath("/assets/mobile-terminal.png")} alt="AnyTTY Android terminal after pairing with a demo workstation" />

## Trust sequence

1. **The daemon creates a short-lived claim.** The offer is bound to the daemon identity, requested capabilities, and a single enrollment flow.
2. **The client scans or pastes the claim and proves its identity.** The native client stores endpoint data and credentials in platform-protected storage.
3. **The daemon issues client-bound access.** Consumption is recorded so the same claim cannot be replayed.

> **Treat an unredeemed QR as a secret.** Do not place pairing URIs in logs, screenshots, issue reports, chat, or version control. Revoke a grant from the daemon if a client is lost or no longer trusted.

Protocol details and failure rules are in the repository's [pairing specification](https://github.com/anytty/anytty/blob/main/docs/PAIRING_PROTOCOL.md).
